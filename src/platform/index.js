import server from './server'
import client from './client'

const config = {
  name: 'Platform',
  description: 'Platform',
  path: ''
}

export { config }
export { server }
export { client }

export default {
  config,
  server,
  client
}
